# Write a python script to convert a str type data into an int type. Also describe when a str type value is not possible to convert into an int type.

a="5"
print(int(a))

b="b"
print(int(b))  # It is not possible to convert this str into int because int() doesn't convert a char into a int type.